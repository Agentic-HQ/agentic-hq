Steve ran:

agentic-hq create-workflow -- --using add-feature

and told AI:

purpose: quick test of creating new workflow from existing, new workflow will add reading from docs/temp-project-design-rules.md for rules about coding during implementation, and auditing those rules were adhered  
  to during new refactoring stage (about the design of code in the project), and will add a refactor stage before verification stage.  workflow-id = test-add-feature-with-rules-and-refactor, plugin-id = same,  
  workflow-short-id = test-add-feature-with-rules-and-refactor. one-sentence-description - you decide based on this

also said later to improve spec at docs/artifacts/workflow-creation-artifacts/agentic-hq-demos-plugin/test-add-feature-with-rules-and-refactor/01-DRAFT-workflow-spec.md this:

I'd like the Refactorer to do more thinking and investigation into what it can refactor and come up with suggestions (not just limited to the Rules Audit). Also needs a Gate where it suggests the stuff       
  (always in a file), gets feedback from the user (in the file) and then proceeds with the Refactoring based on that, and documents it (in the file).


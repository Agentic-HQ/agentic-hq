---
description: Runs a deep stack of skills/commands
disable-model-invocation: true
---

Tell the user your running the run-stack-of-commands skill and are going to call a deep stack of commands.

Then run the:

/steve-test-plugin:sleep-ten-seconds

command.

When it's finished tell the user the exact time (by running a command line app) and that the run-stack-of-commands skill has finished running, with a summary of what happened.